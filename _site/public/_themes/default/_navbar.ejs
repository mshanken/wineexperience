		
		<nav id="menu" class="container menu" role="main-menu">
			<div class="row">
				<div class="col-md-10 col-md-offset-1 hidden-xs">
					<ul class="nav nav-tabs nav-justified">
				<% for(var nav in public._data){ 
					if (nav !== 'sitemap') { %>
						<li class="<%- (current.path[0] == nav) ? 'active' : '' %>"><a href="/<%= nav %>.html"><%= public._data[nav].name %></a></li>
					<% }; %>
				<% }; %>
					</ul>
				</div>
				<div class="col-md-10 col-md-offset-1 visible-xs">
					<ul class="nav nav-tabs nav-stacked">
				<% for(var nav in public._data){ 
					if (nav !== 'sitemap') { %>
						<li class="<%- (current.path[0] == nav) ? 'active' : '' %>"><a href="/<%= nav %>.html"><%= public._data[nav].name %></a></li>
					<% }; %>
				<% }; %>
					</ul>
				</div>
			</div>
		</nav>